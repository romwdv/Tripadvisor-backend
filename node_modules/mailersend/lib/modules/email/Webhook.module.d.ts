import { RequestService, APIResponse } from "../../services/request.service";
import { EmailWebhook, IEmailWebhookUpdateParams } from "../../models";
export declare class EmailWebhookModule extends RequestService {
    constructor(apiKey: string, baseUrl: string);
    create(params: EmailWebhook): Promise<APIResponse>;
    list(domainId: string, queryParams?: {
        limit?: number;
        page?: number;
    }): Promise<APIResponse>;
    single(webhookId: string): Promise<APIResponse>;
    update(webhookId: string, updates: IEmailWebhookUpdateParams): Promise<APIResponse>;
    delete(webhookId: string): Promise<APIResponse>;
}
